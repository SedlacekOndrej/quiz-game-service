package com.sedlacek.quiz.services;

import com.sedlacek.quiz.models.User;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class CapitalServiceTest {
    //CapitalService capitalService = new CapitalService();

    /*@Test
    public void expAwardTest() {
        User.loggedUser = new User("Noob", "noobism", "noob@noobism.com");
        List<String> testStates = List.of(
                "Francie", "Kosovo", "Slovensko", "Řecko", "Polsko", "Bulharsko", "Itálie", "Švédsko", "Rusko", "Kypr"
        );
        List<String> testAnswers = List.of(
                "Paříž", "Priština", "Bratislav", "Atény", "Varšava", "Sofie", "Řím", "Stockholm", "Moskva", "Nikósie"
        );
        capitalService.playTheQuiz();
        assertEquals(100, User.loggedUser.getExp());
    }*/

}